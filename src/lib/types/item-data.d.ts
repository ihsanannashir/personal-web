export type ProjectData = {
  title: string;
  slug: string;
  description?: string;
  thumbnail?: string | StaticImport;
  tech?: any;
};

export type MenuData = {
  title: string;
  slug: string;
};
